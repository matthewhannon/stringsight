// Archived legacy V2 mockup entry.
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';

import './styles/global.css';
import { PracticeWorkspaceV2 } from './app/PracticeWorkspaceV2';

const rootElement = document.querySelector<HTMLDivElement>('#root');

if (rootElement === null) {
  throw new Error('StringSight could not find its V2 mockup root element.');
}

createRoot(rootElement).render(
  <StrictMode>
    <PracticeWorkspaceV2 />
  </StrictMode>,
);
