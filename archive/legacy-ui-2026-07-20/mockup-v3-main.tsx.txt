// Archived legacy V3 mockup entry.
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';

import './styles/global.css';
import { PracticeWorkspaceV3 } from './app/PracticeWorkspaceV3';

const rootElement = document.querySelector<HTMLDivElement>('#root');

if (rootElement === null) throw new Error('StringSight could not find its V3 root.');

createRoot(rootElement).render(<StrictMode><PracticeWorkspaceV3 /></StrictMode>);
